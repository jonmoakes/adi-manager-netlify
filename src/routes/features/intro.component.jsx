import { Div } from "../../styles/div/div.styles";

const Intro = () => (
  <Div>
    <p>
      all the following sections are optional, so you can pick and choose which
      data you want to store!
    </p>
    <p>Tap on the images to view them in full size.</p>
  </Div>
);

export default Intro;
